name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    # We use the explicit subfolder name from your repo structure
    defaults:
      run:
        working-directory: TransparentCalendarWidget-5e950a682d81f9e1f856ef52100fdfdc03b2e070

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17' # Matches the project's language level
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TransparentCalendar-Debug
          # Path must point to the APK within the subfolder structure
          path: TransparentCalendarWidget-5e950a682d81f9e1f856ef52100fdfdc03b2e070/app/build/outputs/apk/debug/*.apk
